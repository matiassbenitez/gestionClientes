<h1 class="my-4 text-center text-primary"><%= title %></h1> 
<%- include('partials/_debts-form', {debt: debt}) %>
<% if (debts && debts.length > 0) { %>
  <h2>Deudas</h2>
  <%- include('partials/_debts-table', { debts: debts }) %>
<% } %>
